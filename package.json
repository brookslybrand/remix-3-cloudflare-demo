{
  "name": "remix-3-cloudflare-demo",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "deploy": "wrangler deploy",
    "dev": "wrangler dev",
    "start": "wrangler dev",
    "typecheck": "pnpm run cf-typegen && tsc --noEmit",
    "cf-typegen": "wrangler types",
    "format": "prettier --write \"src/**/*.{ts,tsx,js,jsx,json}\"",
    "format:check": "prettier --check \"src/**/*.{ts,tsx,js,jsx,json}\""
  },
  "devDependencies": {
    "prettier": "^3.6.2",
    "typescript": "^5.5.2",
    "wrangler": "^4.49.0"
  },
  "dependencies": {
    "@remix-run/cookie": "^0.4.0",
    "@remix-run/fetch-router": "^0.9.0",
    "@remix-run/html-template": "^0.3.0",
    "@remix-run/route-pattern": "^0.15.0",
    "@remix-run/session": "^0.2.0"
  }
}
